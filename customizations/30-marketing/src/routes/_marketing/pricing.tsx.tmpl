// Pricing page — placeholder only. The user fills in tiers later. There are
// deliberately no Free/Pro/Enterprise scaffolds, no feature lists, no CTAs:
// default content is noise that downstream agents have to identify and remove.
import { createFileRoute } from '@tanstack/react-router';
import { seoHead } from '../../components/SeoHead';

export const Route = createFileRoute('/_marketing/pricing')({
  component: PricingPage,
  head: () => seoHead({ title: 'Pricing' }),
});

function PricingPage() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-16">
      <h1 className="text-2xl font-semibold text-gray-900">Pricing</h1>
      <p className="mt-4 text-gray-600">Pricing goes here.</p>
    </div>
  );
}
