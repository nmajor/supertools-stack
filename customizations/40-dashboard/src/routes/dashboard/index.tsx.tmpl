// Dashboard index — placeholder only. No widgets, no metric cards, no welcome
// banner. The body is "Dashboard goes here." by design — see HANDOFF "Dashboard
// pages are empty placeholders" content philosophy. Future agents drop real
// content here once the user has a product.
import { createFileRoute } from '@tanstack/react-router';
import { seoHead } from '../../components/SeoHead';

export const Route = createFileRoute('/dashboard/')({
  component: DashboardIndex,
  head: () => seoHead({ title: 'Dashboard' }),
});

function DashboardIndex() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-16">
      <h1 className="text-2xl font-semibold text-gray-900">Dashboard</h1>
      <p className="mt-4 text-gray-600">Dashboard goes here.</p>
    </div>
  );
}
